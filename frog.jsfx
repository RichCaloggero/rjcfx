desc:rjc frog

slider1:s_enable=1 <0, 1, 1 {off, on}>enable

@init
function foo () (
this.x = 7;
this.y = 66;
);

@sample

s_enable ? (
do.foo();
) : (
do.x = 1;
do.y = 1;
);

sprintf(#dbg_desc, "values: %f, %f", do.x, do.y);

